# Known Issues / Paused Work

This file is `@`-imported from the repo's `CLAUDE.md`, so it loads into every session automatically. Kept as a separate file anyway (rather than inlined into `CLAUDE.md`) so it can keep growing as a running issue log without cluttering the main instructions file's own structure.
